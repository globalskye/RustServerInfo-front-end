
import {createTheme} from '@mui/material/styles'
const themeDark = createTheme({
    palette: {
      background: {
        default: "grey",
      },
      text: {
        primary: "#ffffff",
      },
    },
  });

export {themeDark};